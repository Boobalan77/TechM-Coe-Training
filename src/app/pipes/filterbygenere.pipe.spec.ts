import { FilterbygenerePipe } from './filterbygenere.pipe';

describe('FilterbygenerePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbygenerePipe();
    expect(pipe).toBeTruthy();
  });
});
